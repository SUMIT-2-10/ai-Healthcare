import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─── Brand — medical green ─────────────────────────────────────────────────
  static const Color primary = Color(0xFF2E7D32);
  static const Color primaryDark = Color(0xFF1B5E20);
  static const Color primaryLight = Color(0xFFE8F5E9);
  static const Color primarySoft = Color(0xFFF1F8F2);
  static const Color accent = Color(0xFF43A047);

  // ─── Trust — medical blue ──────────────────────────────────────────────────
  static const Color trust = Color(0xFF1976D2);
  static const Color trustDark = Color(0xFF0D47A1);
  static const Color trustLight = Color(0xFFE3F2FD);

  // ─── Paper ─────────────────────────────────────────────────────────────────
  static const Color background = Color(0xFFF7F9F7);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF0F3F0);
  static const Color surfaceSunken = Color(0xFFEAEEEA);

  // ─── Text ──────────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF0F1A12);
  static const Color textSecondary = Color(0xFF4B5550);
  static const Color textMuted = Color(0xFF7C837E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ─── Triage categories ─────────────────────────────────────────────────────
  // Emergency
  static const Color emergency = Color(0xFFD32F2F);
  static const Color emergencyDeep = Color(0xFFB71C1C);
  static const Color emergencyLight = Color(0xFFFFEBEE);

  // Doctor Visit
  static const Color doctorVisit = Color(0xFFF57C00);
  static const Color doctorVisitDeep = Color(0xFFE65100);
  static const Color doctorVisitLight = Color(0xFFFFF3E0);

  // Home Care
  static const Color homeCare = Color(0xFF2E7D32);
  static const Color homeCareDeep = Color(0xFF1B5E20);
  static const Color homeCareLight = Color(0xFFE8F5E9);

  // ─── Symptom chip pastels ──────────────────────────────────────────────────
  static const Color chipFeverBg = Color(0xFFFFF3E0);
  static const Color chipFeverFg = Color(0xFFE65100);
  static const Color chipHeartBg = Color(0xFFFFEBEE);
  static const Color chipHeartFg = Color(0xFFC62828);
  static const Color chipStomachBg = Color(0xFFE0F7FA);
  static const Color chipStomachFg = Color(0xFF00796B);
  static const Color chipHeadBg = Color(0xFFF3E5F5);
  static const Color chipHeadFg = Color(0xFF6A1B9A);
  static const Color chipChildBg = Color(0xFFE3F2FD);
  static const Color chipChildFg = Color(0xFF1565C0);

  // ─── Border + utility ──────────────────────────────────────────────────────
  static const Color border = Color(0x1A0F1A12);
  static const Color borderStrong = Color(0x26000000);
  static const Color divider = Color(0x14000000);
  static const Color shadow = Color(0x14000000);
  static const Color shadowElevated = Color(0x1F000000);
  static const Color white = Color(0xFFFFFFFF);
}
