import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFF3D6B4F);
  static const Color primaryDark = Color(0xFF2E5A3F);
  static const Color primaryLight = Color(0xFFE8F2EC);
  static const Color accent = Color(0xFF5B8C6E);

  // Background
  static const Color background = Color(0xFFF5F0E8);
  static const Color surface = Color(0xFFFDFAF4);
  static const Color surfaceAlt = Color(0xFFEDE8DC);

  // Text
  static const Color textPrimary = Color(0xFF1A1A14);
  static const Color textSecondary = Color(0xFF5C5A50);
  static const Color textMuted = Color(0xFF9C9A8E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Triage — Emergency
  static const Color emergency = Color(0xFFD32F2F);
  static const Color emergencyLight = Color(0xFFFFEBEE);
  static const Color emergencyBorder = Color(0xFFEF9A9A);

  // Triage — Doctor Visit
  static const Color doctorVisit = Color(0xFFF57F17);
  static const Color doctorVisitLight = Color(0xFFFFF8E1);
  static const Color doctorVisitBorder = Color(0xFFFFE082);

  // Triage — Home Care
  static const Color homeCare = Color(0xFF2E7D32);
  static const Color homeCareLight = Color(0xFFE8F5E9);
  static const Color homeCareBorder = Color(0xFFA5D6A7);

  // Border
  static const Color border = Color(0x265A584B);
  static const Color borderStrong = Color(0x4D5A584B);

  // Utility
  static const Color white = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE0DDD4);
  static const Color shadow = Color(0x1A000000);
}
